#include "vg_define.h"

layout(local_size_x = VG_GROUP_SIZE, local_size_y = 1, local_size_z = 1) in;
void main()
{
	uint globalIndex = gl_GlobalInvocationID.x;
	if (globalIndex < hierarchyDataNum)
	{
		if (ClusterFixupUpload[globalIndex] != 0XFFFFFFFF)
		{
			ClusterHierarchy[globalIndex].isLeaf = ClusterFixupUpload[globalIndex];
		}
	}
}